# Books

**CashlessConsumer reference books** — definitive, independently researched works on fintech, digital public infrastructure, and consumer finance.

Each book lives in `books/<slug>/` as a set of per-chapter markdown files. A CI pipeline builds PDF, EPUB, MOBI, and HTML on every push to `main` and publishes them to GitHub Pages.

**Browse the library:** https://ccagentorg.github.io/books

---

## Quick Start (for contributors)

```bash
# Clone
git clone https://github.com/CCAgentOrg/books.git
cd books

# Build one book (all formats)
bash scripts/build-book.sh books/aadhaar-esign

# Build all books
bash scripts/build-all.sh
```

Outputs land in `output/`. The library website lives in `docs/`.

---

## Repository Structure

```
.
├── AGENTS.md                      # Ground rules, workflow, style guide
├── books/
│   ├── aadhaar-esign/             # Book: Aadhaar eSign: The Definitive Reference
│   │   ├── AGENTS.md              # Book-specific agent instructions
│   │   ├── README.md              # Contributor guide for this book
│   │   ├── metadata.yaml          # Title, author, description, rights
│   │   ├── chapter-index.yml      # Ordered list of parts + chapter files
│   │   ├── manuscript/
│   │   │   ├── 00-frontmatter.md  # Title page, contents
│   │   │   ├── ch01.md            # Chapter 1
│   │   │   ├── ch02.md            # Chapter 2
│   │   │   ├── ...
│   │   │   ├── 90-about-this-book.md
│   │   │   └── 91-about-the-author.md
│   │   └── manuscript.md          # (optional) monolithic fallback
│   └── <next-book>/               # Future books follow the same pattern
├── scripts/
│   ├── build-book.sh              # Build one book (PDF + EPUB + MOBI + HTML + MD)
│   ├── build-all.sh               # Build all books + generate library page
│   ├── generate-library.py        # Create docs/index.html from output/
│   └── update-books-json.py       # Generate machine-readable books index
├── assets/
│   └── book.css                   # Shared book reading styles
├── docs/
│   ├── index.html                 # Library landing page (auto-generated by CI)
│   ├── books/                     # Built output per book
│   └── books.json                 # Machine-readable book index
├── .github/workflows/
│   └── build-pdfs.yml             # CI: build all books → deploy to Pages
└── .gitignore
```

---

## Creating a New Book

1. **Create a branch:** `book/<slug>` (e.g. `book/upi-101`)
2. **Add files:**
   - `books/<slug>/metadata.yaml` — book metadata
   - `books/<slug>/chapter-index.yml` — ordered chapter list
   - `books/<slug>/manuscript/` — per-chapter markdown files
   - `books/<slug>/AGENTS.md` — book-specific agent instructions
   - `books/<slug>/README.md` — contributor guide for this book
3. **Build locally:** `bash scripts/build-book.sh books/<slug>`
4. **Open a PR** to `main`
5. **Merge** → CI builds all formats → deploys to Pages

See `AGENTS.md` at repo root for full conventions on structure, voice, citations, and quality checklist.

---

## Output Formats

| Format | Extension | Primary Tool |
|--------|-----------|-------------|
| PDF | `.pdf` | pandoc + Eisvogel (XeLaTeX) |
| EPUB | `.epub` | pandoc |
| MOBI | `.mobi` | calibre (from EPUB) |
| HTML | `.html` | pandoc + custom CSS |
| Markdown | `.md` | source (concatenated) |

---

## License

All books published under **CC BY-NC-SA 4.0** unless otherwise noted in the book's `metadata.yaml`.

Built with pandoc, XeLaTeX, Eisvogel template, and GitHub Actions.
